# APKs gerados (usar nomeados para distribuição, não commitar os brutos)
*.apk

# Diretórios de build e cache do sistema
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/jdk17/

# Variáveis de ambiente (credenciais)
app/.env
.env

# Android local config
app/android/local.properties
app/android/.gradle/

# macOS
.DS_Store
